import type { ColumnMap } from '@/shared/lib/excel/types'

export const assignAdaptationColumnMap: ColumnMap = [
	['Сотрудник', 'fullname'],
	['Должность', 'position_name'],
	['Отдел', 'position_parent_name']
]
